<div class="container">
  <div class="row">
     <div class="col">
       {{#each posts as |post|}}
       <div class="m-4">
          {{> post-info post}}
       </div>
       {{/each}}
     </div>
  </div>
</div>